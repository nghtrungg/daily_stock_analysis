# Discord Bot Configuration

Configure Discord notifications with `DISCORD_WEBHOOK_URL`. Discord receives report or alert messages according to notification routing. A Discord failure should be reported as notification delivery failure, not analysis failure. Do not log full webhook URLs.

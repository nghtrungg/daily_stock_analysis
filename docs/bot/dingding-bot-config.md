# DingTalk Bot Configuration

Configure DingTalk notifications with `DINGTALK_WEBHOOK_URL` and optional `DINGTALK_SECRET`. DingTalk delivery is routed through the notification service. Failures should be isolated from the main analysis flow unless fail-fast is explicitly requested.

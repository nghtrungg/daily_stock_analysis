---
applyTo: "README.md,docs/**/*.md"
---

# Removed Client Surface

This repository has no Web, HTTP API, Desktop, Docker, or cloud-deployment client surface. Documentation must describe the CLI analyzer, bots, notifications, scheduling, and SQLite storage only. Do not add client build or deployment instructions unless an explicit requirement restores that surface.

